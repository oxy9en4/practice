#include "stdafx.h"
#include "StudentManager.h"

#include "stdafx.h"
#include "StudentManager.h"

int main() {
    LinkedList<int> list;
    list.Push_Back(5);
    list.Push_Back(10);
    list.Push_Back(15);
    list.Push_Back(20);

    return 0;
}

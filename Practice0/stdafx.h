#pragma once
#include <iostream>
#include <math.h>
#include <vector>
#include <queue>
#include <string>
#include <set>
#include <Windows.h>

#define randstep(fmin, fmax) ((float)fmin + ((float)fmax - (float)fmin)* rand() / RAND_MAX)